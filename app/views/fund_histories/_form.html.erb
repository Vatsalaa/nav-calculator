<%= form_with(model: fund_history, local: true) do |form| %>
  <% if fund_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fund_history.errors.count, "error") %> prohibited this fund_history from being saved:</h2>

      <ul>
      <% fund_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :nav %>
    <%= form.text_field :nav %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
